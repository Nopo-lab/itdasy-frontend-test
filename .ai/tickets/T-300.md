# T-300 · Phase 6.3 Lane D — 엑셀 AI 임포트 v2

**상태:** executed (retroactive)
**담당:** 오케스트레이터 (연준 지시 직접 집행)
**의존:** 없음 (기존 imports 라우터 확장)
**브랜치:** `main` 직접 (⚠️ 워크플로우 위반 — 소급 문서화)
**위험 라벨:** `risk:core-flow` (신규 BE 모듈 + FE 위저드)

## ⚠️ 워크플로우 위반 경고

이 티켓은 **2026-04-21~22 오케스트레이터가 AGENTS.md §4 표준 트랙을 무시하고 직접 집행한 작업의 소급(retroactive) 문서화**. 향후 엄격히 준수 약속.

## 목표

임의 양식의 엑셀(CSV/XLSX) 파일을 AI(Gemini) 가 해석해 우리 스키마로 자동 매핑 + 중복 감지 + 미리보기 + 행별 처리 정책 선택.

## Acceptance Criteria

- [x] `services/smart_importer.py` 신설 — Gemini JSON 매핑 + 중복 bulk 감지
- [x] `/imports/ai/analyze` — 헤더·샘플·매핑 제안·confidence·extras·duplicates 반환
- [x] `/imports/ai/commit` — mapping + extras_action + dup_policy 적용 저장
- [x] `app-import-wizard.js` FE — 4단계 위저드 (업로드→매핑→중복→반영)
- [x] 파워뷰 "📥 엑셀 불러오기" 버튼과 연결
- [x] 중복 기본 정책 `skip` (안전 우선)
- [x] 매출 중복 키 강화: `(customer+amount+date+service)` 복합

## 영향 파일

**BE (신규/수정):**
- `backend/services/smart_importer.py` (신규, 약 260줄)
- `backend/routers/imports.py` (+98줄, `/imports/ai/*` 추가)

**FE (신규/수정):**
- `itdasy-frontend-test-yeunjun/app-import-wizard.js` (신규, 약 230줄)
- `itdasy-frontend-test-yeunjun/app-power-view.js` (수정, 위저드 연결)
- `itdasy-frontend-test-yeunjun/index.html` (스크립트 등록)

## 커밋 히스토리

- BE staging `d3d5ef8` (2026-04-21) · FE `eb65e58` (2026-04-21)

## 리스크 / 알려진 한계

1. Gemini 매핑은 확률적 — `ai_confidence < 0.7` 항목은 사용자 확인 권장
2. `.xls` (구버전 binary) 미지원 — xlsx 로 변환 안내
3. 엑셀 내 이미지 셀은 무시됨
4. Customer 50명 Free 한도 계속 적용

## 후속 작업

- 부분 반영 (50명 초과 시 앞 50명만) — T-310 예정
- 임포트 후 되돌리기(rollback) 는 기존 `/imports/jobs/{id}/rollback` 재활용

## Verification

- CBT4 계정에서 CSV 3종 (한국어 헤더 / 영어 헤더 / 혼합 헤더) 업로드 → 매핑 정확도 확인
- 중복 5건 이상 파일 업로드 → 라디오로 skip/overwrite/new_row 각각 선택 후 반영 결과 일치
