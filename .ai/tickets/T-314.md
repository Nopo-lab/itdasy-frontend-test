# T-314 · 자동 로그인 — 토큰 유효기간 연장 + 만료 자동 감지

**상태:** pending (연준 🟢 2026-04-22)
**담당:** 오케스트레이터
**위험 라벨:** `risk:auth-flow` (토큰 정책 변경)

## 배경

현재 토큰 만료 **24시간** (1440분). 원장님이 하루만 앱 안 열어도 재로그인 필요 → UX 떨어짐.
프론트는 이미 토큰 있으면 자동 로그인 작동 중 (app-core.js `if(getToken())`).
문제는 "토큰 만료" 체감.

## 목표

30일 유지 + 만료 시 자동 감지 후 부드러운 재로그인 유도.

## Acceptance Criteria

- [ ] BE `ACCESS_TOKEN_EXPIRE_MINUTES` 기본값 1440 → 43200 (30일)
- [ ] Railway staging/prod env 동일 반영
- [ ] FE: 토큰 만료 401 응답 시 `sessionExpiredMsg` 표시하고 lockOverlay 재노출
- [ ] 앱 foreground 복귀 시 `/auth/me` ping 으로 토큰 조용히 검증 (이미 일부 구현됨 — 보완)

## 영향 파일

- `backend/utils/security.py` — 기본 만료 상수
- `app-core.js` — 만료 감지 시 재로그인 유도 (기존 sessionExpiredMsg 재활용)

## 리스크

1. 30일은 UX 편이지만 기기 분실 시 악용 창 넓음 → **T-317 생체 인증 도입 후 15일로 재축소** 권장
2. JWT 는 무상태라 서버에서 단일 사용자 토큰 무효화 불가 — 기존 blacklist 시스템 활용 (회원 탈퇴 시)
3. 운영·스테이징 env 둘 다 반영 필요

## Verification

1. 새 계정 가입 → 로그인 → 앱 끄고 1일 후 재진입 → 바로 대시보드 (재로그인 없음)
2. 30일 이후 재진입 → "로그인 세션이 만료되었습니다" 표시 후 재로그인 폼
