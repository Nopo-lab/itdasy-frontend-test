# T-330 · 구 페르소나 탭 + 레거시/숨김/노쇼 일괄 대청소

**상태:** done ✅ (원영님 🟢 2026-04-22 "실행해")
**담당:** 오케스트레이터 (Cowork 단일)
**트랙:** 표준 (약 1,700줄 순삭감)
**브랜치:** `main` (Cowork 직접 편집 후 사용자가 push)
**위험 라벨:** `risk:ui-only` (프론트 전용, 백엔드는 별도 T-330-B)

> 상세 plan/self-review: `.ai/tickets/T-330/plan.md`, `.ai/tickets/T-330/self-review.md`
> 감사 원본: `.ai/AUDIT_2026-04-22.md`

## 목표

2026-04-20 이후 **신 글쓰기 온보딩** (`showOnboardingCaptionPopup` → POST `/shop/persona/onboarding`) 가 구 페르소나 탭 Q1~Q9 설문을 완전히 대체. 이중 로직 제거 + 감사에서 나온 소규모 죽은 코드 일괄 정리.

원영님 지시: "신 글쓰기 로직에만 집중, 구 페르소나 탭 전부 삭제, 한 티켓에 묶어."

🚫 **불가침:** 글쓰기 탭 시나리오 팝업 (`openCaptionScenarioPopup` · `scenario-selector.js` · `_doGenerateCaption`) — 원영님 "이 로직 최고". 에러 핸들러 문구 1군데 외 수정 금지.

## 실행 결과 (스코프별)

| 스코프 | 내용 | 상태 |
|---|---|---|
| A | 구 페르소나 탭 제거: `app-persona.js`(900줄) · `components/persona-popup.js`(544줄) · `js/persona/CLAUDE.md` 삭제 · `app-caption.js` identity_incomplete 핸들러 신 온보딩 교체 · `app-instagram.js` 자동 페르소나 팝업 2군데 제거 · `style-home.css` `.header-persona-texts`/`persona-status-*` 정리 · `.eslintrc.js` 예외 목록 정리 | ✅ |
| B | 쿠키 동의 제거: `app-cookie-consent.js`(49줄) 삭제 · index.html 주석 정리 · README 파일 표 갱신 | ✅ |
| C | 숨김 UI 정리: `index.html` `homeQuestion` dead span 제거 · cbt1ResetArea 는 CBT 테스트용이라 **유지** (app-core.js:1077 주석 근거) | ✅ |
| D | 레거시 토큰 블록 제거: `app-core.js` `migrateLegacyToken` IIFE (10줄) 삭제 · `_TOKEN_KEY` 패턴만 유지 | ✅ |
| E | 노쇼 프론트 UI 제거: `app-booking.js`(버튼·라벨) · `app-report.js`(컬럼 3→2) · `app-calendar-view.js`(statusColor 매핑) · `app-customer-dashboard.js`(아이콘 분기) 일괄 정리 | ✅ |
| F | CLAUDE.md 하네스 슬림화: 루트 85→16줄 + `js/{caption,core,gallery,portfolio}/CLAUDE.md` 각 ≤20줄 | ✅ |

**백엔드 노쇼 제거는 별도 티켓** → `.ai/tickets/T-330-B-backend.md` (연준 전달)

## 보존한 것 (중요 — 착각 금지)

| 대상 | 이유 |
|---|---|
| `components/scenario-selector.js` | 신 글쓰기 `openCaptionScenarioPopup` 에서 사용 (app-caption.js:491, 521) |
| `index.html #personaDash` div | `app-instagram.js:renderPersonaDash` 가 신 온보딩 톤 카드 렌더하는 타겟 |
| `cbt1ResetArea` 버튼 | yeunjun/test 레포 전용 CBT 테스트 장비 (운영에서만 숨김 처리됨) |

## 검증

- `grep -r "app-persona\|persona-popup\|openPersonaPopup\|initPersonaTab\|personaRoot\|tab-persona\|headerPersonaName" .` → 0건 ✅
- `grep -r "no_show\|noshow\|bf-status.*no" js/ app-*.js` → 0건 (프론트) ✅
- `grep -r "renderScenarioSelector\|showOnboardingCaptionPopup\|renderPersonaDash" app-caption.js app-instagram.js` → 신 로직 호출 정상 ✅
- CLAUDE.md 모두 ≤20줄 ✅ (`wc -l js/*/CLAUDE.md CLAUDE.md`)

## 백엔드 연계

**연준 레포 (`itdasy_backend-test`) 에서 처리 필요 — T-330-B 티켓 참조**:
- `/bookings/{id}/noshow` 엔드포인트 (혹은 유사명) 제거
- `BookingStatus` enum 에서 `no_show` 값 제거
- 관련 마이그레이션 + 문서 정리

프론트는 이미 해당 호출 전부 제거됨 → 백엔드가 정리되면 500/404 없이 완결.

## 영향 파일 요약

**삭제 (3):** `app-persona.js` · `components/persona-popup.js` · `app-cookie-consent.js`
**수정 (주요):** `index.html` · `app-core.js` · `app-caption.js` · `app-instagram.js` · `app-booking.js` · `app-report.js` · `app-calendar-view.js` · `app-customer-dashboard.js` · `style-home.css` · `.eslintrc.js` · `README.md`
**CLAUDE.md (5):** 루트 + `js/{caption,core,gallery,portfolio}/`
