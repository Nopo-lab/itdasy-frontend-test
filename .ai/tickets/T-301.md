# T-301 · Phase 6.3 Lane E — AI 챗봇 9능력 확장 + PIPA 가명처리

**상태:** executed (retroactive)
**담당:** 오케스트레이터 (직접 집행)
**의존:** 없음
**브랜치:** `main` 직접 (⚠️ 워크플로우 위반)
**위험 라벨:** `risk:ai-personal-data` (개인정보 국외이전 관련)

## 목표

AI 비서를 "조회 전용" 에서 **실행형 만능 비서** 로 확장. 동시에 PIPA 국외이전 방어로 고객 이름 가명처리.

## Acceptance Criteria

- [x] SYSTEM_PROMPT 개편 — A(복합)/B(검색)/C(슬롯)/D(캡션)/E(코칭)/F(이상감지)/G(일괄메시지)/H(예약수정·취소·이동)/I(페르소나기억)/K(인사이트)/L(리스트)/M(필드수정)/N(고객요약)/P(DM초안) 14종 모드
- [x] 새 액션 kind 7종: `update_booking`, `cancel_booking`, `reschedule_booking`, `update_customer`, `generate_bulk_message`, `upsert_inventory`, (브리핑용 `/brief`)
- [x] `/assistant/brief` 엔드포인트 — 이상 감지 요약
- [x] `/assistant/style` — 사용자 말투 선호 저장/조회
- [x] 전화번호 파싱 강화 (11자리 01x / 10자리 지역번호만 phone, 그 외는 memo)
- [x] `related_questions` 필드 추가 + FE 말풍선 렌더
- [x] answer 이중 직렬화 방어 (중첩 JSON 가드)
- [x] TZ-aware datetime 버그 픽스 (`_collect_context`, `compute_at_risk`)
- [x] **PIPA 가명처리** — Gemini 전송 시 고객명 → `고객#1` 치환, 응답 시 역복원

## 영향 파일

**BE:**
- `backend/routers/assistant.py` (+약 400줄)
- `backend/models.py` (+`ShopSettings.assistant_style`, `card_fee_rate`)
- `backend/main.py` (+컬럼 마이그레이션)
- `backend/schemas/shop.py` (+필드)
- `backend/services/retention_predictor.py` (TZ 픽스)
- `backend/routers/customers.py` (`_format_phone` 추가 · TZ 픽스)
- `backend/routers/revenue.py` (`_compute_fee` 사용자 설정 수수료율 적용)

**FE:**
- `app-assistant.js` — 새 action 카드 7종, IME 픽스, 연관질문 렌더
- `app-killer-widgets.js` — `/brief` 소비 + SMS 프리필 모달

## 커밋 히스토리

- BE `c9e9159`, `55047a2`, `48659f3`
- FE `30314b8`, `40aa6b2`

## 리스크

1. **Gemini 정책 변경** 시 JSON schema enum 재점검 필요
2. 가명처리는 context 내 이름만 — 사용자가 채팅에 직접 쓴 이름은 그대로 전송 (처리방침 명시)
3. Gemini 호출 비용 증가 (월 추정 $10~30 for CBT 50명)
4. SYSTEM_PROMPT 길이 2500+ tokens — 응답 지연 가능성

## Verification

- 챗봇 시나리오 9가지 통과 (L, M, N, P 포함)
- 전화번호 케이스 3종: 01012345678 / 5308869 / 8자리 숫자+한글 → 각각 올바른 phone/memo 분리
- `/brief` 응답 today_bookings, at_risk, low_stock, mom_delta_pct 정확
- 가명처리: Railway 로그에 "고객#1" 으로 전송 확인, FE 응답엔 원본 이름
