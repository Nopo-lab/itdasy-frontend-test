# T-312 · 음성 입력 전역화 (모든 텍스트 필드 🎤)

**상태:** pending (연준 🟢 2026-04-22)
**담당:** 오케스트레이터
**의존:** 없음 (Web Speech API 재활용)
**위험 라벨:** `risk:ui-only`

## 목표

모든 `<input type="text">`, `<textarea>` 옆에 🎤 버튼 자동 주입. 탭 시 Web Speech 음성 인식 → 해당 필드 채움.

## Acceptance Criteria

- [ ] `app-voice-input.js` 신규 — MutationObserver 로 DOM 관찰
- [ ] `.pv-input`, `.bf-*`, 일반 input/textarea 자동 감지
- [ ] 🎤 버튼 right-absolute 배치 + 녹음 중 붉은 펄스
- [ ] 한글/영어 인식 (`ko-KR`)
- [ ] opt-out: `data-no-voice` 속성 있으면 주입 안 함
- [ ] iOS Safari 는 Web Speech 미지원 → 자연스러운 숨김

## 영향 파일

- `app-voice-input.js` (신규)
- `index.html` (스크립트 등록)
- `style-polish.css` (펄스 애니메이션)

## 리스크

1. iOS Safari 는 Web Speech API 부분 지원 → Capacitor Voice 플러그인 폴백 검토
2. 민감 입력(비밀번호) 제외 필수

## Verification

1. 파워뷰 입력 행 🎤 탭 → 말하기 → 자동 채움
2. 예약 메모 필드 동일
3. 비밀번호 필드엔 🎤 없음
