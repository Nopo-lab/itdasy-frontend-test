# T-310 · 예약 캘린더 뷰 + 드래그 리스케줄

**상태:** pending (연준 🟢 2026-04-22 "응 해")
**담당:** 오케스트레이터 (T2 FE + T3 BE 겸임)
**의존:** 없음 (기존 /bookings 엔드포인트 재활용)
**브랜치 예정:** `main` (FE 경량) — 1일 내 완료 목표
**위험 라벨:** `risk:ui-only` (백엔드 스키마 무변)

## 목표

예약 생성·수정·취소에서 **타이핑 최소화**. 월/주/일 캘린더 뷰로 빈 슬롯 탭 / 블록 드래그 리스케줄.

## Acceptance Criteria

- [ ] `app-calendar-view.js` 신규
- [ ] FullCalendar.js 6.x CDN 통합 (timeGridWeek / timeGridDay / dayGridMonth)
- [ ] 예약 시트 상단에 **📅 캘린더 / 📋 목록** 토글 버튼
- [ ] 빈 슬롯 탭 → 기존 예약 추가 시트 프리필 (date + start + end)
- [ ] 기존 예약 블록 드래그 → PATCH `/bookings/{id}` 로 starts_at/ends_at 업데이트
- [ ] 블록 리사이즈(끝 드래그) → ends_at 갱신
- [ ] 충돌 시 빨간 테두리 + 저장 거부
- [ ] 한국어 locale · 09:00~22:00 기본 표시

## 영향 파일

**FE 신규/수정:**
- `app-calendar-view.js` (신규, ~200줄)
- `app-booking.js` (토글 버튼 + 캘린더 연동 훅)
- `index.html` (FullCalendar CDN + 신규 스크립트)

**BE:** 변경 없음 (`BookingUpdate.status`·`starts_at`·`ends_at` 이미 PATCH 가능)

## 리스크

1. FullCalendar 번들 크기 ~300KB — 동적 load 로 초기 로딩 영향 최소화
2. 드래그 충돌 감지는 클라이언트측 선검증 → 서버 409 폴백
3. iOS Safari WebKit 드래그 이벤트 미묘 — touch 이벤트 폴백

## Verification

1. 캘린더 뷰에서 빈 슬롯 탭 → 예약 추가 시트 열림
2. 기존 예약 드래그 → 서버 반영 + 캘린더 업데이트
3. 블록 끝 리사이즈 → ends_at 변경
4. 월/주/일 뷰 전환 부드러움
