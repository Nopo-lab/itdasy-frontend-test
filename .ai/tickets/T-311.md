# T-311 · 대시보드 그래프 고도화 + 위젯 드래그 재배치

**상태:** pending (연준 🟢 2026-04-22)
**담당:** 오케스트레이터
**의존:** 없음
**브랜치 예정:** `main`
**위험 라벨:** `risk:ui-only`

## 목표

대시보드 그래프 인터랙션·애니메이션 개선 + 위젯 드래그앤드롭 재배치 + 로컬 저장.

## Acceptance Criteria

- [ ] Chart.js 4.x CDN 통합
- [ ] 기존 SVG 월별 차트 → Chart.js line/bar (호버 툴팁·부드러운 애니메이션)
- [ ] 스파크라인 매출 흐름 위젯 추가
- [ ] Sortable.js 1.x CDN 통합
- [ ] 위젯 컨테이너(`#dashKiller`)에 드래그 재배치
- [ ] localStorage `itdasy_widget_order_v1` 에 순서 저장
- [ ] "꾹 누르기 → 흔들림" 애니메이션 (active 모드)
- [ ] 이징 함수 `cubic-bezier(0.22, 1, 0.36, 1)` 전역 적용
- [ ] 위젯 접기/펼치기 (요약/상세 토글)

## 영향 파일

- `app-killer-widgets.js` (drag 재배치 + 접기 토글)
- `app-dashboard.js` (Chart.js 바 차트 교체)
- `index.html` (CDN 2종)
- `style-polish.css` (흔들림 애니메이션)

## 리스크

1. Chart.js 번들 ~90KB → 초기 로딩 영향 미미
2. Sortable touch 이벤트 iOS 호환 검증 필요
3. 저장 순서 손상 시 기본 순서 폴백

## Verification

1. 대시보드 바 차트 호버 툴팁 표시
2. 위젯 꾹 누르기 → 드래그 → 놓기 → 순서 유지 (새로고침 후에도)
3. 위젯 상단 영역 탭 → 접기·펼치기 전환
